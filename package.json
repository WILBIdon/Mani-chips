{
  "name": "mandi-chips",
  "version": "1.0.0",
  "description": "Catálogo web premium de Mandi Chips",
  "scripts": {
    "start": "npx -y serve . -l $PORT -s",
    "dev": "npx -y serve . -l 3000 -s"
  },
  "engines": {
    "node": ">=18"
  }
}
